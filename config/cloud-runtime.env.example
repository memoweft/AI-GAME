# Optional OpenAI-compatible cloud dialogue/planner.
# Keep this file non-secret. The API key is accepted only from the process
# environment variable AI_GAME_CLOUD_CHAT_API_KEY.
CLOUD_CHAT_ENDPOINT=
CLOUD_CHAT_MODEL=

# Optional process-level tuning:
# AI_GAME_CHAT_TIMEOUT_SECONDS=30
# AI_GAME_CHAT_MAX_WORKERS=2
# AI_GAME_CHAT_MAX_PENDING=32
